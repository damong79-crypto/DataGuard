<Window x:Class="DataGuard.App.Views.QueryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="체크 쿼리 추가" Height="540" Width="560"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="이름" VerticalAlignment="Center" Margin="0,6" />
        <TextBox   Grid.Row="0" Grid.Column="1" x:Name="NameBox" Margin="0,6" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="대상 연결" VerticalAlignment="Center" Margin="0,6" />
        <ComboBox  Grid.Row="1" Grid.Column="1" x:Name="ConnectionBox" Margin="0,6"
                   DisplayMemberPath="Name" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="발송 정책" VerticalAlignment="Center" Margin="0,6" />
        <ComboBox  Grid.Row="2" Grid.Column="1" x:Name="PolicyBox" Margin="0,6" />

        <TextBlock Grid.Row="3" Grid.Column="0" Text="스케줄" VerticalAlignment="Center" Margin="0,6" />
        <ComboBox  Grid.Row="3" Grid.Column="1" x:Name="ScheduleKindBox" Margin="0,6"
                   SelectionChanged="OnScheduleKindChanged">
            <ComboBoxItem Content="수동 실행만" />
            <ComboBoxItem Content="매 N분마다" />
            <ComboBoxItem Content="매일 지정 시각" />
            <ComboBoxItem Content="평일 지정 시각" />
        </ComboBox>

        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,2">
            <TextBlock x:Name="IntervalLabel" Text="간격(분)" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBox   x:Name="IntervalBox" Width="60" VerticalAlignment="Center" Text="60" />
            <TextBlock x:Name="TimeLabel" Text="시각(HH:mm)" VerticalAlignment="Center" Margin="16,0,6,0" />
            <TextBox   x:Name="TimeBox" Width="70" VerticalAlignment="Center" Text="09:00" />
        </StackPanel>

        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Margin="0,8,0,2"
                   Text="SQL (틀어진 행만 반환 — 결과 0건이면 정상)" />
        <TextBox   Grid.Row="6" Grid.ColumnSpan="2" x:Name="SqlBox" Margin="0,2"
                   AcceptsReturn="True" AcceptsTab="True" TextWrapping="NoWrap"
                   VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                   FontFamily="Consolas" />

        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="저장" Width="80" IsDefault="True" Click="OnSave" Margin="0,0,8,0" />
            <Button Content="취소" Width="80" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
